import {Avatar, IconButton, Typography} from "@mui/material";

export function UserBubbleComponent () {

  return (
    <IconButton
      size={'small'}
    >
      <Avatar />
    </IconButton>
  );
}
